# Icons

**Status: TODO**

No standalone icon files exist yet — the current UI uses inline emoji as icons (see the `.dc.html` canvases). Add exported icon assets (SVG/PNG) here if/when a dedicated icon set is built.
