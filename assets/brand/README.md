# Brand Assets

**Status: TODO**

No standalone brand asset files exist yet (logo files, color swatches, wordmark exports). The brand mark today is an inline 🏠 emoji + `#E4572E` orange defined in CSS inside the `.dc.html` canvases (see [`/design/design-system`](../../design/design-system/)). Export real logo/brand files here once they exist.
