# Design System

**Status: DONE (v1.0, Phase 1 scope)**

Open [`BANHAO Design System.dc.html`](./BANHAO%20Design%20System.dc.html) in a browser — it's a self-contained interactive canvas (color, typography, foundation, components, states, voice, tokens). `support.js` is a required local runtime dependency; keep both files together.

See also the written index at [`docs/03-design`](../../docs/03-design/).
