# Customer App

**Status: DONE (Phase 1 flow, 17 screens)**

Open [`BANHAO Customer App.dc.html`](./BANHAO%20Customer%20App.dc.html) in a browser — self-contained interactive canvas covering splash → onboarding → OTP → browse/search → shop → item options → cart → checkout → address → PromptPay QR → order success → tracking → rating → order history → notifications. `support.js` is a required local runtime dependency; keep both files together.

QA feedback screenshots for this flow: [`/assets/screenshots`](../../assets/screenshots/).
