# Customer App

**Status: DONE (Phase 1 flow, 18 screens)**

Open [`BANHAO Customer App.dc.html`](./BANHAO%20Customer%20App.dc.html) in a browser — self-contained interactive canvas covering splash → onboarding → login → OTP → home → search → shop → item options → cart → checkout confirm → delivery address → PromptPay QR → order success → order tracking → rating → order history → notifications → account. `support.js` is a required local runtime dependency; keep both files together.

QA feedback screenshots for this flow: [`/assets/screenshots`](../../assets/screenshots/).
