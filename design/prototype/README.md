# Prototype

**Status: TODO**

Reserved for full end-to-end clickable prototypes or exports that combine multiple app surfaces (e.g. a linked customer→driver→merchant flow), as distinct from the single-surface canvases in the sibling folders. Nothing lives here yet — today each `.dc.html` canvas is self-contained per surface.
