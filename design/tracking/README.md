# Tracking / Map

**Status: DONE (prototype)**

[`tracking-map.html`](./tracking-map.html) — standalone Leaflet map prototype showing shop / customer / driver pins around อ.บุณฑริก. Uses Leaflet from a CDN (unpkg); no local runtime dependency, unlike the `.dc.html` canvases elsewhere in `/design`.
