{
  "name": "@banhao/driver",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "dev": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "echo \"@banhao/driver: no tests yet (foundation only)\" && exit 0"
  },
  "dependencies": {
    "@banhao/api-client": "workspace:*",
    "@banhao/types": "workspace:*",
    "@banhao/ui": "workspace:*",
    "@banhao/validation": "workspace:*",
    "expo": "~52.0.23",
    "expo-status-bar": "~2.0.0",
    "react": "18.3.1",
    "react-native": "0.76.5"
  },
  "devDependencies": {
    "@types/react": "~18.3.12",
    "typescript": "^5.7.2"
  }
}
